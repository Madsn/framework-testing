<!DOCTYPE html>
<html>
<head>
  <title>Shuffle</title>
  <%= render 'layouts/includes/header' %>
</head>
<body>

<%= render 'layouts/includes/navbar' %>

<div class="container">
  <% flash.each do |name, msg| %>
      <div class="alert alert-<%= name %>"><%= msg %></div>
  <% end %>
  <%= yield %>
</div>

<%= render 'layouts/includes/footer' %>
</body>
</html>
